"""Built-in analyses registered in monet_registry (one module per engine).

Each module decorates its functions with monet_registry.analysis; monet_registry.BUILTIN lists
the modules imported at start-up. Third-party analyses go in plugins/ instead (docs/plugins.md).
"""
